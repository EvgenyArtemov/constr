import { createUnit } from '../../utils';

export const filterFormUnit = createUnit({
    component: 'FilterForm',
    'childUnits': [],
    'useMapProps': () => ({ 'text': '123' }),
    'params': { },
    modelFields: null,
    model: null
})